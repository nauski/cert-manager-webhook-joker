apiVersion: v1
kind: Service
metadata:
  name: cert-manager-webhook-joker
  namespace: cert-manager
  labels:
    app: cert-manager-webhook-joker
spec:
  type: ClusterIP
  ports:
    - port: 443
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: cert-manager-webhook-joker